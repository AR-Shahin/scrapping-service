services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: content-extractor-api:latest
    container_name: content-extractor-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      LINKEDIN_USER_DATA_DIR: /home/appuser/.linkedin_browser_profile
